(function(){ var fill = document.getElementById('meterFill'); var num = document.getElementById('meterNum'); var target = 92; var reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches; function run(){ if(reduced){ fill.style.height = target + '%'; num.innerHTML = target + '<span>%</span>'; return; } fill.style.height = target + '%'; var start = null; var duration = 2400; function step(ts){ if(!start) start = ts; var progress = Math.min((ts - start) / duration, 1); var eased = 1 - Math.pow(1 - progress, 3); var current = Math.round(eased * target); num.innerHTML = current + '<span>%</span>'; if(progress < 1) requestAnimationFrame(step); } requestAnimationFrame(step); } if(document.readyState === 'complete'){ setTimeout(run, 250); } else { window.addEventListener('load', function(){ setTimeout(run, 250); }); } })();